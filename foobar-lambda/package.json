{
  "name": "foobar-lambda",
  "version": "1.0.0",
  "source": "index.ls",
  "main": "dist/lambda.js",
  "license": "MIT",
  "scripts": {
    "build": "npx parcel build",
    "bundle": "npm run build && zip ../terraform/lambda.js.zip dist/lambda.js"
  },
  "devDependencies": {
    "parcel": "^2.7.0",
    "parcel-transformer-livescript": "^2.0.0"
  }
}
